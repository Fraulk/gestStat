{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{# <div class="example-wrapper">
    <h1>Liste des Statistiques</h1>
</div>

<div class="container">
    <div class="row">
{% for statistique in statistiques %}
        <div class="col-md-4">
            <div class="card mb-4 shadow-sm">
                <div class="card-body">
                    <p class="card-text">{{statistique.titre}}</p>
                    <p>Categorie : {{statistique.categorie.libelle}}</p>
                    <p>Description : {{statistique.description}}</p>
                    <p>Prix : {{statistique.price}}</p>
                    <p>Prix : {{statistique.CreatedAt | date("d/m/Y") }}</p>

                </div>
            </div>
        </div>
{% endfor %}
    </div>
</div> #}
{% endblock %}
